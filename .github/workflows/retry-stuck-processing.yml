name: Retry Stuck Processing Files

on:
  schedule:
    # Check for stuck files every 10 minutes
    - cron: "*/10 * * * *"
  workflow_dispatch: {}

jobs:
  retry-stuck-files:
    runs-on: ubuntu-latest
    steps:
      - name: Reset Stuck Processing Files
        run: |
          curl -sS -X POST http://169.231.231.63:3000/api/upload/reset-stuck-processing
